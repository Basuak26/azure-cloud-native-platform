trigger:
  branches:
    include:
      - main

variables:
  - group: azure-platform-vars
  - name: azureResourceManagerConnection
    value: 'ArmConnection'

stages:
- stage: Deploy_DEV
  displayName: 'Deploy to DEV Environment'
  variables:
    envName: 'dev'
    rgName: 'rg-azplat-dev'
    location: 'eastus'
  jobs:
  - job: ProvisionAndBuild
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    # 1. Provision Infrastructure First (Uses Bicep default images)
    - task: AzureResourceManagerTemplateDeployment@3
      displayName: 'Provision Infrastructure (Bicep)'
      inputs:
        deploymentScope: 'Resource Group'
        azureResourceManagerConnection: '$(azureResourceManagerConnection)'
        subscriptionId: '$(subscriptionId)'
        action: 'Create Or Update Resource Group'
        resourceGroupName: '$(rgName)'
        location: '$(location)'
        templateLocation: 'Linked artifact'
        csmFile: 'infra/main.bicep'
        overrideParameters: '-envName $(envName)'
        deploymentMode: 'Incremental'
        deploymentOutputs: 'bicepOutputs'

    # 2. Extract ACR Login Server output from Bicep
    - task: PowerShell@2
      displayName: 'Parse Bicep Outputs'
      inputs:
        targetType: 'inline'
        script: |
          $outputs = ConvertFrom-Json '$(bicepOutputs)'
          $acrLoginServer = $outputs.acrLoginServer.value
          Write-Host "##vso[task.setvariable variable=acrLoginServer]$acrLoginServer"
          Write-Host "Dynamically fetched ACR: $acrLoginServer"

    # 3. Build & Push using standard Azure CLI (No Docker connection needed!)
    - task: AzureCLI@2
      displayName: 'Build and Push Docker Images'
      inputs:
        azureSubscription: '$(azureResourceManagerConnection)'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Log into ACR using ARM credentials
          ACR_NAME=$(echo $(acrLoginServer) | cut -d'.' -f1)
          az acr login --name $ACR_NAME
          
          # Build and push API
          docker build -f src/Api/Dockerfile -t $(acrLoginServer)/api:$(Build.BuildId) src/Api
          docker push $(acrLoginServer)/api:$(Build.BuildId)
          
          # Build and push Worker
          docker build -f src/Worker/Dockerfile -t $(acrLoginServer)/worker:$(Build.BuildId) src/Worker
          docker push $(acrLoginServer)/worker:$(Build.BuildId)

    # 4. Update the Container Apps with our newly pushed images
    - task: AzureCLI@2
      displayName: 'Update Container Apps with new code'
      inputs:
        azureSubscription: '$(azureResourceManagerConnection)'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          az containerapp update --name ca-api-$(envName) --resource-group $(rgName) --image $(acrLoginServer)/api:$(Build.BuildId)
          az containerapp update --name ca-worker-$(envName) --resource-group $(rgName) --image $(acrLoginServer)/worker:$(Build.BuildId)
